<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ncvt.speed.mapper.AccountMapper">

    <resultMap id="UserRMap" type="com.ncvt.speed.entity.AccountEntity">
        <result column="user_id" property="userId"></result>
        <result column="user_name" property="userName"></result>
        <result column="password" property="password"></result>
        <result column="salt" property="salt"></result>
        <result column="belong_id" property="belongId"></result>
    </resultMap>

    <select id="queryOneAccount" resultMap="UserRMap">
        SELECT user_id,user_name,password,salt,belong_id
        FROM tb_account WHERE user_name=#{userName}
    </select>

    <!-- keyProperty="userId" 设置主键字段对应的实体类 -->
    <insert id="accountAddition" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO tb_account(
            user_name,
            password,
            salt,
            belong_id
        ) VALUES (
            #{userName},#{password},#{salt},#{belongId}
        )
    </insert>

    <!-- 用户名修改 -->
    <update id="updateUserName">
        UPDATE tb_account SET user_name=#{userName} WHERE user_id=#{userId}
    </update>

    <!-- 查询登录密码 -->
    <select id="queryAccountByPassword" resultMap="UserRMap">
        SELECT password,salt FROM tb_account WHERE user_id=#{userId}
    </select>

    <!-- 登录密码修改 -->
    <update id="updateUserByPassword">
        UPDATE tb_account SET password=#{password} WHERE user_id=#{userId}
    </update>

</mapper>