<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ncvt.speed.mapper.AccountMapper">

    <resultMap id="UserRMap" type="com.ncvt.speed.entity.AccountEntity">
        <result column="user_id" property="userId"></result>
        <result column="user_name" property="userName"></result>
        <result column="password" property="password"></result>
        <result column="salt" property="salt"></result>
    </resultMap>


    <select id="queryOneAccount" resultMap="UserRMap">
        SELECT * FROM tb_account WHERE user_name=#{userName}
    </select>

    <!-- keyProperty="userId" 设置主键字段对应的实体类 -->
    <insert id="accountAddition" useGeneratedKeys="true">
        INSERT INTO tb_account(
            user_name,
            password,
            salt
        ) VALUES (
                     #{userName},#{password},#{salt}
                 )
    </insert>



</mapper>